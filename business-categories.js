// Icon shown for a business when it has no logo of its own (see businesses/README.md).
// Add an entry here per `category` value to give it its own icon.
const CATEGORY_ICONS = {
  "Pet Shop": "🐶",
};

const DEFAULT_CATEGORY_ICON = "🏢";
